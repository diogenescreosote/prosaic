# Signatures and timestamp proofs

One `.sig.asc` (detached GPG signature) per executed document, and
`.ots` OpenTimestamps proofs anchoring when each signature existed.
Verification instructions: `../HOW TO VERIFY.md`.
