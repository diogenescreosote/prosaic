# assets/ — Evidence records, exhibit-ready and AI-triageable

This file is the authoritative description of everything under
`assets/`; any change to the tree must update it in the same change.

Conventions (see matter CLAUDE.md): every PDF text-searchable (original
or `_ocr` sibling) with a `.txt` sidecar; images have
verified-transcription sidecars; audio has speaker-attributed local
transcripts; originals are never modified.
