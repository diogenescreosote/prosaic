reportlab
pyyaml
# The Judicial Council form PDFs are AES-encrypted, so reading them
# needs pypdf's crypto extra. It was never declared: on a developer's
# machine `cryptography` arrives transitively via ocrmypdf's pdfminer,
# and form filling appeared to work. In a clean environment it raises
# "cryptography>=3.1 is required for AES algorithm".
pypdf[crypto]
python-docx
Pillow
pytest
# Trusted timestamps for sc attest (the `ots` CLI). A signature's
# claimed time is signer-asserted; OpenTimestamps anchors it in a
# public blockchain so a backdated signature is detectable. attest
# degrades to a stderr note if this is somehow absent.
opentimestamps-client
# Official DocuSeal API client (sc esign): submissions from PDF,
# status, signed-document retrieval. docuseal.com/docs/api
docuseal
